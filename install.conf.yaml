- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.bashrc: 
    ~/.bash_logout: 
    ~/.bash_aliases: 
    ~/.inputrc: 
    ~/.tmux.conf: 
    ~/.vimrc: 
    ~/.functions: 

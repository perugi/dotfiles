- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- create:
    - ~/.scripts
    - ~/.vim/undo-history

- link:
    ~/.bashrc: 
    ~/.bash_logout: 
    ~/.inputrc: 
    ~/.tmux.conf: 
    ~/.vimrc: 
    ~/.config/starship.toml: 
    ~/.config/fish/config.fish:
    ~/.config/fish/alias.fish:
    ~/.config/fish/functions/mkd.fish: fish_functions/mkd.fish
    ~/.scripts/functions: functions
    ~/.scripts/backup: backup
